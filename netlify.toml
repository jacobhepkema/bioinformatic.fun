[build]
publish = "public"
command = "hugo --gc --minify"

[context.deploy-preview.environment]
  HUGO_VERSION = "0.53"
  HUGO_ENV = "production"

[context.deploy-preview.environment]
  HUGO_VERSION = "0.53"
